// Имя корневого backend-проекта.
rootProject.name = "notes-backend"

// Репозитории для резолва зависимостей всех модулей.
dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
